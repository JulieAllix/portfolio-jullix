import React from "react";
import styled from "styled-components";

interface Props {
    value: number;
}

export const TabsPager: React.FC<Props> = (props) => {

    return (
        <TabsPagerWrapper>

        </TabsPagerWrapper>
    )
}

const TabsPagerWrapper = styled.div`

`;
